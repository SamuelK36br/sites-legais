import save from './save.js';

const { backgroundColor, clothingColor, eyesColor, glassesColor, hairColor, hatColor, mouthColor } = save.colors;

const colors = {
	backgroundColor: backgroundColor,
	clothingColor: clothingColor,
	eyesColor: eyesColor,
	glassesColor: glassesColor,
	hairColor: hairColor,
	hatColor: hatColor,
	mouthColor: mouthColor,
}

export default colors;